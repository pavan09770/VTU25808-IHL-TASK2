[project]
name = "story-mcp"
version = "0.1.0"
description = "MCP server for saving AI-generated creative stories"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "huggingface-hub>=1.27.0",
    "mcp[cli]",
    "python-dotenv>=1.2.2",
]
